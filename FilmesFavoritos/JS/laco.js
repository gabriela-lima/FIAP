//Lacos de repeticao
// for, while, do while (principais) (ainda tem o foreach e etc)(entendendo os basicos entende o resto)

// construindo loop para tabuadas

for (let i = 0; i <= 10; i++){
    for (let j = 0; j <= 10; j++)
    {
        console.log(`${i} * ${j} = ${i * j}`);
    }
}